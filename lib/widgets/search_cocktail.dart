import 'package:flutter/material.dart';

class SearchCocktail extends StatelessWidget {
  const SearchCocktail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
